name: flip_card_swiper
description: A customizable, swipeable card widget with smooth flip animations and haptic support.
version: 2.1.0
repository: https://github.com/LiquidatorCoder/flip_card_swiper
topics: [animation, card, swipe, transform, flip]

environment:
  sdk: ">=3.11.1 <4.0.0"
  flutter: ">=3.41.4"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

screenshots:
  - description: 3D edge flip effect.
    path: screenshots/3d.gif
  - description: Credit card style flip.
    path: screenshots/credit.gif
  - description: Minimal style flip.
    path: screenshots/minimal.gif

flutter:
